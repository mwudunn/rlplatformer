from setuptools import setup

setup(name='gym_platformer',
        version='0.0.1',
        install_requires=['gym', 'numpy']
)
setup(
    name='QLearning',
    version='0.1.0',
    packages=['QLearning'],
)